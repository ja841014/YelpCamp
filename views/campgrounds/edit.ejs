<% layout('layouts/boilerplate') %> 

    <h1>Edit campground</h1>
    <form action="/campgrounds/<%= campground._id%>?_method=PUT" method = "POST">
        <div>
            <label for="title">Title</label>
            <input type="texr" id="title" name="campground[title]" value="<%= campground.title %> ">
        </div>
        <div>
            <label for="location">Location</label>
            <input type="texr" id="location" name="campground[location]" value="<%= campground.location %> ">

        </div>
        <button>edit Campground</button>
        <footer>
            <a href="/campgrounds/<%= campground._id %>"> back to campgrounds</a>
        </footer>
    </form>
